name: tenniscourtcare
description: "Court Care - Application de suivi de maintenance des courts de tennis"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7


dependencies:
  flutter:
    sdk: flutter

  intl: ^0.19.0   # ou la dernière version disponible
  http: ^1.3.0

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  # Riverpod v2
  flutter_riverpod: ^2.6.1
  
  # Drift (SQLite)
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0
  go_router: ^17.1.0
  flutter_secure_storage: ^10.0.0
  crypto: ^3.0.7
  fl_chart: ^1.1.1
  google_fonts: ^8.0.2
  table_calendar: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Build runner pour Drift
  build_runner: ^2.4.11
  drift_dev: ^2.18.0

flutter:
  uses-material-design: true
